#include <windows.h>

int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR lpCmdLine, int nCmdShow) {
    // Simple window creation logic here
    MessageBox(NULL, "GameBooster UI Initialized", "Status", MB_OK);
    return 0;
}
